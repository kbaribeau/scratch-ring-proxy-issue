(ns scratch-ring-proxy-issue.core-test
  (:require [clojure.test :refer :all]
            [scratch-ring-proxy-issue.core :refer :all]))

(deftest a-test
  (testing "FIXME, I fail."
    (is (= 0 1))))
